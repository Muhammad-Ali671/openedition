apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  type: NodePort
  selector:
    app: myapp
  ports:
  - port: 80          # External port
    targetPort: 8080  # Tomcat inside container
    nodePort: 30080   # Fixed NodePort for easy access
